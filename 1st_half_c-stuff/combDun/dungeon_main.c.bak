#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <dungion_header.h>

int main(){
	//this main will get removed once more of the code is writen
	int k;
	srand(time(NULL));
	k = dungion_map();
	return k;
}